import { TexDirective } from './tex.directive';

describe('TexDirective', () => {
  it('should create an instance', () => {
    const directive = new TexDirective(null);
    expect(directive).toBeTruthy();
  });
});
